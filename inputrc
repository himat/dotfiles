$include /etc/inputrc

# Incremental history navigation with up/down keys using already typed text
#   as a prefix
"\e[A": history-search-backward
"\e[B": history-search-forward

# Default tab behavior (just including for completeness) that displays matches
TAB: complete

## Tab-complete options
# Shift-tab cycles through files automatically
"\e[Z":menu-complete
# Auto completes the first matching prefix
set menu-complete-display-prefix on
# Displays all tab completion options instead of choosing the first one automatically
#set show-all-if-ambiguous on

# For bash auto completion colors, doesn't seem to be working for me though
set colored-stats on
set colored-completion-prefix on # Shows colored prefixes when multiple items match a completion

