git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm

echo "Enter tmux and then Press prefix + I (capital i, as in Install) to install all the plugins."
echo "If you're already in a tmux session, then run `tmux source ~/.tmux.conf` first to apply any changes"
